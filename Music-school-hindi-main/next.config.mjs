/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com', "imgd.aeplcdn.com","news.webster.edu"]
    }
};

export default nextConfig;
